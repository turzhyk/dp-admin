version: "3.9"

services:
  react-app:
    build: .
    container_name: DPAdmin
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true